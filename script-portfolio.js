document.addEventListener('DOMContentLoaded', function(){
    const toggleButtons = document.querySelectorAll('expand-button');
    const textContainers = document.querySelectorAll('portfolio-text');
    const hiddenDivs = document.querySelectorAll('portfolio-hidden-text');

   
});
